# Arquivo — documentação histórica (era Firebase)

Estes arquivos são da fase em que o app usava **Firebase Auth + Realtime Database**.

O backend atual é **somente Supabase**. Use a documentação ativa em `docs/`:

| Ativo | Em vez de |
|-------|-----------|
| `SUPABASE-MIGRACAO.md` | setup Firebase Console |
| `SUPABASE-PERFIS.md` | `FIREBASE-PERFIS.md` |
| `CADASTRO-SUPABASE.md` | `CADASTRO-FIREBASE.md` |
| `supabase/schema.sql` + `seed.sql` | `database.rules.json` / seed JSON |
| `PARA-O-MENTOR.md` | handoff atual |

As pastas `FASE-*-ESTUDO.md` e a trilha descrevem o aprendizado com Firebase — mantidas só como histórico.
